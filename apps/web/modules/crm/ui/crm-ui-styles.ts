/**
 * Primary actions across CRM — violet → indigo gradient (matches bulk email / campaign studio).
 * Use on `<Button className={CRM_PRIMARY_BTN} />` (overrides default variant background).
 */
export const CRM_PRIMARY_BTN =
    "bg-gradient-to-r from-violet-600 to-indigo-600 text-white shadow-md shadow-violet-500/15 hover:from-violet-500 hover:to-indigo-500 focus-visible:ring-violet-500/40";
